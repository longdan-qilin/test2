2. 提交步骤

```
git add .
git commit -m '说明信息'
git push -u origin 分支名称

后面又修改了代码

git add .
git commit -m '说明信息'
git push
```

3. 开分支

```
git checkout -b 分支名称
git add .
git commit -m '提示信息'
git push -u origin 分支名称

后面又修改了代码
git add .
git commit -m '提示信息'
git push
```